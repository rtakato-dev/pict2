## 仕様メモ

- exeにする
- pictのオプションが使える
- 因子水準が並び替えられる
- yamlファイルでデフォルトの設定を変更可能
  - オプションを付けなくても設定でオプションを常につけて実行できる
- ディシジョンテーブルに変換できるオプション
  - yamlで変更可能
- デフォルトの組み合わせを全網羅にする
  - yamlで変更可能
- 制約を付けた時に全網羅との比較をできるオプションを付ける
  - yamlで設定可能
  - 今は実装の優先度が低い
- 期待結果を記述できる
  - 今は実装の優先度が低い

